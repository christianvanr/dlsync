CREATE OR REPLACE FUNCTION ${EXAMPLE_DB}.${MY_SCHEMA}.MY_FUNCTION("ID_TO_FIND" VARCHAR)
RETURNS BOOLEAN
LANGUAGE SQL
AS '
    CASE
        WHERE (SELECT COUNT(ID) FROM ${EXAMPLE_DB}.${MY_SCHEMA}.MY_TABLE WHERE ID = ID_TO_FIND) = 1 THEN TRUE
    ELSE
        FALSE
    END
'